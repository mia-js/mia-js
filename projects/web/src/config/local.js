/**
 * Please be aware of the fact that this file is going to be packed into the frontend bundle so everybody can see it's
 * contents. Don't add access credentials in here!
 */
module.exports = {
  api: {
    root: 'http://localhost:3000/demo/'
  }
}
